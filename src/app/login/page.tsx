import LoginForm from "@/components/loginForm";

const LoginPage = () => {
  return (
    <div className="login">
      <h1>Welcome to Login Page</h1>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
